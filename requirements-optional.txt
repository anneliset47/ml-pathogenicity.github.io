# Optional dependencies not required for core reproducibility.
# Install with: pip install -r requirements-optional.txt
# Use this when running: python run_pipeline.py --all --with-nn

tensorflow==2.21.0
